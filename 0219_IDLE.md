# 방치형게임만들기
> 순서 : 해상도 설정> 맵배치> 플레이어배치> 몬스터소환> 투사체날리기> 피격시 진행할이벤트

> Animator 기능을 활용> isIDLE/isMOVE 셋팅

> insideUnitSphere

> Spawner.cs 오브젝트 풀링을 통해 생성방식에 대한 개선

> 통합매니저 설계(BaseManager) - 모든 매니저에 대한 연결점
  - 해당 매니저에 대한 접근(프로퍼티 구현) : 싱글톤부터 작업

> PoolManager설계 :
- 1. IPool 인터페이스 설계 : 풀에 대한 틀을 제공하는 용도
  - 트랜스폼(풀 연결 위치)
  - 큐(풀 구현)
  - 몬스터 하나 얻어오는 기능
  - 반납하는 기능
- 2. 오브젝트 풀 클래스 설계 : IPool ->
  - 인터페이스 기반으로 설계 완료
- 3. 풀 매니저 맴버 설계 (변수나 함수)
  - string, IPool(PoolManager전에 미리 짜여진)을 Key, Value로 가지는 딕셔너리 (pool_dict) 등록
  - 경로를 전달받아 키를 추가하는 함수 구현
  - 경로를 전달받아 오브젝트를 생성하고 지정된 트랜스폼 쪽에 연결해주는 함수구현

> ! 경로에 대한 정보로 설계를 진행해서 매니저 쪽에 추가 코드 작성

> 매니저코드 수정

> v풀매니저> addQ로 enqueu하는 기능 추가
